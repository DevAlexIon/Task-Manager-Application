{
  "devDependencies": {
    "eslint": "^8.24.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-jsx-a11y": "^6.6.1",
    "eslint-plugin-react": "^7.31.8",
    "eslint-plugin-react-hooks": "^4.6.0",
    "lerna": "^5.5.4"
  },
  "name": "ion-alexandru",
  "version": "1.0.0",
  "scripts": {
    "dev": "lerna run dev",
    "lint": "lerna run lint",
    "client-lint": "lerna run lint --scope web-client",
    "server-lint": "lerna run lint --scope server-api",
    "frontend-unit-test": "lerna run test --scope web-client",
    "backend-unit-test": "lerna run test --scope server-api"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "axios": "^1.0.0"
  }
}
